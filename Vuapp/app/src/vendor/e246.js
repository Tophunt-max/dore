// Verbatim from the ORich reference bundle (webpack module "e246").

// Library internals - regenerate with tools/orich instead of editing.
/* eslint-disable */
import { webpackModule, interop } from './runtime.js';

const deps = {
};

const mod = webpackModule(function (t, e, s) {"use strict";function i(){return uni.getSystemInfoSync().platform}function a(){return uni.getSystemInfoSync()}Object.defineProperty(e,"__esModule",{value:!0}),e.os=i,e.sys=a}, deps);

/** The module's exports object. */
export default mod;

/** What babel's interop helper resolved this require to. */
export const interopDefault = interop(mod);
